ja:
  activerecord:
    errors:
      models:
        post:
          attributes:
            base:
              contetn_attachment_byte_size_is_too_big: 添付ファイルは%{max_content_attachment_mega_byte_size}MB以下にしてください。[%{bytes}/%{max_bytes}]
            title:
              blank: が空です
              too_long: が%{count}文字を超えています
            content:
              too_long: が%{max_content_length}文字を超えています。[%{length}/%{max_content_length}]
              invalid: が%{max_attachable_size_mb}MBを超えています。[%{size}/%{max_attachable_size}]
    attributes:
      post:
        title: タイトル
        content: コンテンツ